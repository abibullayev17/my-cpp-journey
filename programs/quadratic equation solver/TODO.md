For the equation:
ax² + bx + c = 0

Steps:
1) Read a, b, and c
2) Compute the discriminant:
   D = b*b - 4*a*c

3) If D > 0 → two roots (x1, x2)
   If D == 0 → one root (x)
   If D < 0 → no real roots