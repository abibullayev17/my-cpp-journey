#include <iostream>

int main() {
    // array = a data structure that can hold multiple values 
    // values can be accessed by an index number
    // in order to assign values to an array later, we have to assign the size first
    
    std::string car[] = {"Corvette", "Chevrolet", "Toyota"};

    std::cout << car[0];

    return 0;
}